# Production runtime settings for packaged Sklad.
# Copy to .env.production or .env.production.local and replace placeholder values.

DATABASE_URL="postgresql://postgres:change-me@localhost:5432/sklad?schema=public"
JWT_SECRET="replace-with-a-secure-random-string-at-least-32-characters"

# Optional application settings:
# GEMINI_API_KEY=""
# APP_URL=""

# Do not include ALLOW_DEV_AUTH_BYPASS in production.
